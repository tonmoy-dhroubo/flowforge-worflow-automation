spring:
  application:
    name: auth
  datasource:
    url: jdbc:postgresql://localhost:5432/authdb
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

server:
  port: 8081

jwt:
  secret: your-secret-key-here-should-be-at-least-256-bits