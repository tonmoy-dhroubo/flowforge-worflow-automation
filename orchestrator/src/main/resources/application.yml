server:
  port: 8084
spring:
  application:
    name: orchestrator-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/flowforge}
    username: ${SPRING_DATASOURCE_USERNAME:flowforge}
    password: ${SPRING_DATASOURCE_PASSWORD:password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: orchestrator
        hbm2ddl:
          create_namespaces: true
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: orchestrator-group
      auto-offset-reset: earliest
      # FIXED: Use StringDeserializer to handle multiple DTO types on different topics
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
app:
  services:
    workflow:
      url: ${WORKFLOW_SERVICE_URL:http://localhost:8082/api/v1}
  kafka:
    topics:
      trigger-events: trigger.events
      execution-start: execution.start
      execution-result: execution.result
logging:
  level:
    com.flowforge.orchestrator: DEBUG
    org.springframework.kafka: INFO
